(()=>{
'use strict';
const CONFIG_KEY='shooking2_firebase_config',ROOM_KEY='shooking2_online_room',VERSION='10.12.5',MAX_PLAYERS=4;
let app=null,auth=null,db=null,roomCode='',roomRef=null,playerRef=null,currentRoom=null,roomUnsub=null,playersUnsub=null,chatUnsub=null,heartbeat=null,currentPlayers=[],ending=false,stageOpened=false,reconnecting=false;
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),now=()=>firebase.firestore.FieldValue.serverTimestamp();
function status(text,error=false){const el=$('onlineStatus');if(!el)return;el.innerHTML=text;el.style.borderColor=error?'#ef4444':'#38bdf8'}
function config(){try{return JSON.parse(localStorage.getItem(CONFIG_KEY)||'null')}catch{return null}}
function valid(c){return !!(c?.apiKey&&c?.authDomain&&c?.projectId&&c?.appId)}
function load(src,id){return new Promise((res,rej)=>{if($(id))return res();const s=document.createElement('script');s.id=id;s.src=src;s.onload=res;s.onerror=()=>rej(new Error('Firebase SDKを読み込めません'));document.head.appendChild(s)})}
async function ensureFirebase(){if(db&&auth)return;const c=config();if(!valid(c))throw new Error('設定画面でFirebase設定を保存してください');await load(`https://www.gstatic.com/firebasejs/${VERSION}/firebase-app-compat.js`,'firebaseAppSdk');await load(`https://www.gstatic.com/firebasejs/${VERSION}/firebase-auth-compat.js`,'firebaseAuthSdk');await load(`https://www.gstatic.com/firebasejs/${VERSION}/firebase-firestore-compat.js`,'firebaseFirestoreSdk');app=firebase.apps.length?firebase.app():firebase.initializeApp(c);auth=firebase.auth();db=firebase.firestore();await auth.setPersistence(firebase.auth.Auth.Persistence.LOCAL);if(!auth.currentUser)throw new Error('Googleログインしてからオンラインへ参加してください')}
function playerName(){const typed=($('onlineName')?.value||'').trim();if(typed)return typed.slice(0,18);let saved=null;try{saved=JSON.parse(localStorage.getItem('shooking2_current_account')||'null')}catch{}return String(saved?.accountName||auth?.currentUser?.displayName||'PLAYER').slice(0,18)}
function makeCode(){const chars='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';let s='';for(let i=0;i<6;i++)s+=chars[Math.floor(Math.random()*chars.length)];return s}
function installUi(){const screen=$('onlinePlay'),panel=screen?.querySelector('.panel');if(!panel||$('pveRoomControls'))return;panel.innerHTML=`<h1>オンラインPvE協力</h1><p class="small">最大4人。部屋コードで参加し、全員準備後にホストが開始します。</p><div id="pveRoomControls"><input id="onlineName" maxlength="18" placeholder="プレイヤー名"><div style="display:grid;grid-template-columns:1fr 1fr;gap:10px"><button onclick="createPveRoom()">部屋を作る</button><button onclick="quickJoinPveRoom()">クイック参加</button></div><input id="roomName" maxlength="6" placeholder="6文字の部屋コード" style="text-transform:uppercase"><button onclick="joinOnlineRoom()">コードで参加</button></div><div class="onlineCard" id="onlineStatus">未接続</div><div id="pvePlayerList" class="onlineCard" style="text-align:left;margin-top:10px">参加者はいません</div><button id="pveReadyButton" onclick="togglePveReady()" disabled>準備OK</button><button id="pveStartButton" onclick="startGameFromOnline()" disabled>協力ミッション開始</button><div class="chatbox" id="chatBox" style="margin-top:10px"></div><input id="chatInput" maxlength="100" placeholder="メッセージ（100文字まで）"><button onclick="sendChat()">送信</button><button onclick="leavePveRoom()" class="back">ルームから退出</button><button class="back" onclick="openScreen('home')">ホームへ戻る</button>`;$('chatInput')?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();send()}});restoreRoom()}
async function createRoom(){try{await ensureFirebase();await disconnectLocal(false);const code=makeCode(),ref=db.collection('pveRooms').doc(code);await ref.set({code,hostUid:auth.currentUser.uid,status:'lobby',stage:1,maxPlayers:MAX_PLAYERS,createdAt:now(),updatedAt:now()});await enterRoom(code,true)}catch(e){status('作成エラー：'+e.message,true)}}
async function quickJoin(){try{await ensureFirebase();const snap=await db.collection('pveRooms').where('status','==','lobby').limit(12).get();for(const d of snap.docs){const ps=await d.ref.collection('players').get();if(ps.size<MAX_PLAYERS){await enterRoom(d.id,false);return}}await createRoom()}catch(e){status('クイック参加エラー：'+e.message,true)}}
async function joinByCode(){const code=($('roomName')?.value||'').trim().toUpperCase();if(!/^[A-Z2-9]{6}$/.test(code))return status('6文字の部屋コードを入力してください。',true);try{await ensureFirebase();await enterRoom(code,false)}catch(e){status('参加エラー：'+e.message,true)}}
async function enterRoom(code,created=false){ending=false;stageOpened=false;if(roomCode&&roomCode!==code)await disconnectLocal(false);const ref=db.collection('pveRooms').doc(code),rs=await ref.get();if(!rs.exists)throw new Error('部屋が見つかりません');const state=rs.data().status;if(['ended','closed'].includes(state))throw new Error('この部屋は終了しています');const ps=await ref.collection('players').get();if(ps.size>=MAX_PLAYERS&&!ps.docs.some(d=>d.id===auth.currentUser.uid))throw new Error('この部屋は満員です');roomCode=code;roomRef=ref;currentRoom=rs.data();playerRef=ref.collection('players').doc(auth.currentUser.uid);await playerRef.set({uid:auth.currentUser.uid,name:playerName(),online:true,inGame:state==='playing',joinedAt:now(),lastSeen:now()},{merge:true});localStorage.setItem(ROOM_KEY,code);subscribeRoom();startHeartbeat();status(`接続中：<b>${esc(code)}</b>　${created?'あなたがホストです':'参加しました'}`);if($('roomName'))$('roomName').value=code}
function startHeartbeat(){clearInterval(heartbeat);heartbeat=setInterval(async()=>{if(!playerRef)return;try{await playerRef.set({online:true,lastSeen:now()},{merge:true})}catch{reconnectRoom()}},10000)}
async function reconnectRoom(){if(reconnecting||!roomCode||!db||!auth?.currentUser)return;reconnecting=true;try{const saved=roomCode||localStorage.getItem(ROOM_KEY);if(!saved)return;const ref=db.collection('pveRooms').doc(saved),snap=await ref.get();if(!snap.exists)return;roomRef=ref;currentRoom=snap.data();playerRef=ref.collection('players').doc(auth.currentUser.uid);await playerRef.set({uid:auth.currentUser.uid,name:playerName(),online:true,lastSeen:now()},{merge:true});subscribeRoom();startHeartbeat();status(`再接続しました：<b>${esc(saved)}</b>`)}catch(e){status('再接続中です…',true)}finally{reconnecting=false}}
function subscribeRoom(){roomUnsub?.();playersUnsub?.();chatUnsub?.();roomUnsub=roomRef.onSnapshot(s=>{if(!s.exists){status('部屋が終了しました。',true);disconnectLocal(false);return}currentRoom=s.data();renderPlayers();if(currentRoom.status==='playing'&&currentRoom.startedAt){status(`ミッション開始！ 部屋 <b>${esc(roomCode)}</b>`);playerRef?.set({online:true,inGame:true,lastSeen:now()},{merge:true}).catch(()=>{});showTeamHud();if(!stageOpened&&typeof window.openStageSelect==='function'){stageOpened=true;window.openStageSelect()}}if(['finished','ended','closed'].includes(currentRoom.status)){status('ミッション終了。ルームは維持されています。退出ボタンで退出できます。');playerRef?.set({inGame:false,online:true,lastSeen:now()},{merge:true}).catch(()=>{});window.ShooOnlineTeamFix?.addLog?.('オンラインミッション終了。ルーム接続は維持されています。','ONLINE')}} ,()=>reconnectRoom());playersUnsub=roomRef.collection('players').orderBy('joinedAt').onSnapshot(s=>{currentPlayers=s.docs.map(d=>({id:d.id,...d.data()}));renderPlayers();showTeamHud()},()=>reconnectRoom());chatUnsub=roomRef.collection('messages').orderBy('createdAt','asc').limitToLast(40).onSnapshot(s=>{const box=$('chatBox');if(!box)return;box.innerHTML=s.docs.map(d=>{const m=d.data();return `<div class="chatmsg"><b>${esc(m.name)}</b>：${esc(m.text)}</div>`}).join('');box.scrollTop=box.scrollHeight},()=>reconnectRoom())}
function renderPlayers(){const box=$('pvePlayerList');if(!box)return;const host=currentRoom?.hostUid||'';let html=`<b>ROOM ${esc(roomCode||'------')}</b><br>`;for(let i=0;i<MAX_PLAYERS;i++){const p=currentPlayers[i];html+=p?`<div style="padding:7px 0;border-bottom:1px solid #334155">${p.id===host?'👑':'👤'} ${esc(p.name)}　${p.ready?'✅ 準備OK':'⌛ 待機中'}</div>`:`<div style="padding:7px 0;opacity:.55">□ 空き</div>`}box.innerHTML=html;const me=currentPlayers.find(p=>p.id===auth?.currentUser?.uid),ready=$('pveReadyButton');if(ready){ready.disabled=!me;ready.textContent=me?.ready?'準備を取り消す':'準備OK'}const allReady=currentPlayers.length>0&&currentPlayers.every(p=>p.ready),start=$('pveStartButton');if(start){start.disabled=!(host===auth?.currentUser?.uid&&allReady&&currentRoom?.status==='lobby');start.textContent=allReady?'協力ミッション開始':'全員の準備待ち'}}
async function toggleReady(){if(!playerRef)return status('先に部屋へ参加してください。',true);const s=await playerRef.get();await playerRef.set({ready:!s.data()?.ready,lastSeen:now()},{merge:true})}
async function send(){const input=$('chatInput'),text=(input?.value||'').trim();if(!text||!roomRef)return;await roomRef.collection('messages').add({uid:auth.currentUser.uid,name:playerName(),text:text.slice(0,100),createdAt:now()});input.value=''}
async function start(){if(!roomRef)return status('部屋へ参加してください。',true);const rs=await roomRef.get();if(rs.data()?.hostUid!==auth.currentUser.uid)return status('開始できるのはホストだけです。',true);const ps=await roomRef.collection('players').get();if(!ps.size||ps.docs.some(d=>!d.data().ready))return status('全員が準備OKになるまで開始できません。',true);await roomRef.update({status:'playing',startedAt:now(),updatedAt:now()})}
function showTeamHud(){let hud=$('pveTeamHud');if(!roomCode){hud?.remove();return}if(!hud){hud=document.createElement('div');hud.id='pveTeamHud';hud.style.cssText='position:fixed;right:10px;top:70px;z-index:4;background:rgba(2,6,23,.78);border:1px solid #38bdf8;border-radius:12px;padding:9px;font:12px system-ui;max-width:190px';document.body.appendChild(hud)}hud.innerHTML='<b>ONLINE TEAM</b>'+currentPlayers.map(p=>`<div>${p.down||p.alive===false?'🔴':'🟢'} ${esc(p.name)}</div>`).join('')}
async function endMission(result='finished'){if(ending||!roomRef)return;ending=true;try{const snap=await roomRef.get();if(snap.exists&&snap.data()?.hostUid===auth?.currentUser?.uid)await roomRef.set({status:'finished',result:String(result).slice(0,40),finishedAt:now(),updatedAt:now()},{merge:true});await playerRef?.set({inGame:false,online:true,finishedAt:now(),lastSeen:now()},{merge:true})}catch{}ending=false;window.ShooOnlineTeamFix?.addLog?.('ミッション終了。ルーム接続は維持されています。','ONLINE')}
async function explicitLeave(){await disconnectLocal(true);window.ShooMultiplayerSync?.disconnect?.();window.ShooOnlineTeamFix?.addLog?.('部屋から退出しました。','ONLINE')}
async function disconnectLocal(removePlayer=false){clearInterval(heartbeat);heartbeat=null;roomUnsub?.();playersUnsub?.();chatUnsub?.();roomUnsub=playersUnsub=chatUnsub=null;if(playerRef){try{if(removePlayer)await playerRef.delete();else await playerRef.set({online:false,inGame:false,lastSeen:now()},{merge:true})}catch{}}roomCode='';roomRef=playerRef=null;currentRoom=null;currentPlayers=[];stageOpened=false;localStorage.removeItem(ROOM_KEY);$('pveTeamHud')?.remove();$('visibleOnlineTeam')?.remove();$('pveWorldOverlay')?.remove();if($('pvePlayerList'))$('pvePlayerList').textContent='参加者はいません';status('未接続')}
async function restoreRoom(){const saved=roomCode||localStorage.getItem(ROOM_KEY);if(!saved)return;try{await ensureFirebase();const r=await db.collection('pveRooms').doc(saved).get();if(!r.exists||['ended','closed'].includes(r.data().status)){localStorage.removeItem(ROOM_KEY);return}await enterRoom(saved,false)}catch(e){status('ルーム再接続待ち：'+e.message,true)}}
window.createPveRoom=createRoom;window.quickJoinPveRoom=quickJoin;window.joinOnlineRoom=joinByCode;window.togglePveReady=toggleReady;window.sendChat=send;window.startGameFromOnline=start;window.leavePveRoom=explicitLeave;window.ShooOnlineRoom={endMission,leave:explicitLeave,reconnect:reconnectRoom,touch:()=>playerRef?.set({online:true,lastSeen:now()},{merge:true}),get code(){return roomCode},get ref(){return roomRef},get playerRef(){return playerRef},get players(){return currentPlayers}};
window.addEventListener('shoo:mission-ended',e=>endMission(e.detail?.result||'finished'));
window.addEventListener('beforeunload',()=>{if(playerRef)playerRef.set({online:false,inGame:false,lastSeen:now()},{merge:true}).catch(()=>{})});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installUi);else installUi();
})();