const CACHE_NAME="shooking-pages-v101";
const BUILD="101";
const RAW_BASE="https://raw.githubusercontent.com/sasahokofamily-debug/sora-geme-app/main/";
const SCOPE_URL=new URL(self.registration.scope);
const SCOPE_PATH=SCOPE_URL.pathname.replace(/\/+$/,"")+"/";
self.addEventListener("install",event=>event.waitUntil(self.skipWaiting()));
self.addEventListener("activate",event=>event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(key=>key!==CACHE_NAME).map(key=>caches.delete(key)));await self.clients.claim()})()));
function relativePath(url){const path=url.pathname.startsWith(SCOPE_PATH)?url.pathname.slice(SCOPE_PATH.length):url.pathname.replace(/^\/+/,"");return decodeURIComponent(path||"index.html")}
function contentType(path){const ext=(path.split(".").pop()||"").toLowerCase();return({html:"text/html; charset=utf-8",js:"text/javascript; charset=utf-8",css:"text/css; charset=utf-8",json:"application/json; charset=utf-8",webmanifest:"application/manifest+json; charset=utf-8",svg:"image/svg+xml",png:"image/png",jpg:"image/jpeg",jpeg:"image/jpeg",webp:"image/webp",mp3:"audio/mpeg",wav:"audio/wav"})[ext]||"application/octet-stream"}
async function proxyRootFile(path){const clean=String(path||"index.html").replace(/^\/+/,"").replace(/\.\.(?:\/|\\)/g,"");const response=await fetch(`${RAW_BASE}${clean}?build=${BUILD}`,{cache:"no-store"});if(!response.ok)throw new Error(`HTTP ${response.status}: ${clean}`);const headers=new Headers(response.headers);headers.set("content-type",contentType(clean));headers.set("cache-control","no-store, no-cache, must-revalidate");headers.delete("content-disposition");headers.delete("x-content-type-options");return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}
async function networkFirst(path,key){try{const response=await proxyRootFile(path);const copy=response.clone();caches.open(CACHE_NAME).then(cache=>cache.put(key,copy)).catch(()=>{});return response}catch(error){const cached=await caches.match(key);if(cached)return cached;throw error}}
const PUBLIC_BOOT=`<script id="shookingPublicAuthFix" src="./auth-session-fix.js?v=2"><\/script><script id="shookingPublicAuthGuard">(()=>{const KEY='shooking2_current_account';const has=()=>{try{return!!JSON.parse(localStorage.getItem(KEY)||'null')}catch{return false}};if(has())return;const style=document.createElement('style');style.textContent='html.shooking-auth-pending .screen{visibility:hidden!important;pointer-events:none!important}';document.head.appendChild(style);document.documentElement.classList.add('shooking-auth-pending');const show=()=>{if(has()){document.documentElement.classList.remove('shooking-auth-pending');style.remove();return}const login=document.getElementById('loginScreen');if(!login)return;document.querySelectorAll('.screen').forEach(screen=>screen.classList.add('hidden'));login.classList.remove('hidden');document.getElementById('home')?.classList.add('hidden');document.documentElement.classList.remove('shooking-auth-pending');style.remove()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',show,{once:true});else show()})();<\/script>`;
async function injectGameBoot(response){let html=await response.text();html=html.replace(/<body([^>]*)>/i,match=>match+PUBLIC_BOOT);const headers=new Headers(response.headers);headers.delete("content-length");headers.set("content-type","text/html; charset=utf-8");headers.set("cache-control","no-store, no-cache, must-revalidate");return new Response(html,{status:response.status,statusText:response.statusText,headers})}
function assetFailure(path){const type=contentType(path),message=`SHOO KING II asset load failed: ${path}`;if(type.startsWith("text/javascript"))return new Response(`console.error(${JSON.stringify(message)});`,{status:503,headers:{"content-type":type,"cache-control":"no-store"}});if(type.startsWith("text/css"))return new Response(`/* ${message} */`,{status:503,headers:{"content-type":type,"cache-control":"no-store"}});return new Response(message,{status:503,headers:{"content-type":"text/plain; charset=utf-8","cache-control":"no-store"}})}
self.addEventListener("fetch",event=>{if(event.request.method!=="GET")return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;const rel=relativePath(url);if(event.request.mode==="navigate"){const game=rel==="game"||rel==="game/"||rel==="index.html"||url.searchParams.get("play")==="1";const store=rel==="webstore.html"||rel==="webstore";if(game){event.respondWith(networkFirst("index.html",new Request(`${self.registration.scope}__game_v101.html`)).then(injectGameBoot));return}if(store){event.respondWith(networkFirst("webstore.html",new Request(`${self.registration.scope}__webstore_v101.html`)));return}return}if(!rel||rel==="sw.js")return;event.respondWith(networkFirst(rel,event.request).catch(()=>assetFailure(rel)))});