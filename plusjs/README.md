# plusjs

SHOO KING II の追加UI・拡張機能用 JavaScript をまとめるフォルダです。

現在の主な内容:
- `current-ui-extra.js` — 各サブ画面の現行Command UI拡張
- `login-command-extras.js` — ログイン画面の設定/HOME導線追加

今後、既存機能を壊さず後付けする拡張JSは、できるだけこのフォルダへ追加します。
